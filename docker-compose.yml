services:


  postgres:
    image: postgres:16
    ports:
      - "5433:5432"
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: bmarket
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: